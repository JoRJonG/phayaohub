# Database Setup Guide

## 📊 โครงสร้างฐานข้อมูล

ฐานข้อมูล Phayao Hub ประกอบด้วยตารางหลักดังนี้:

### ตารางหลัก
- **users** - ข้อมูลผู้ใช้งาน
- **categories** - หมวดหมู่สินค้าและงาน
- **market_items** - สินค้าตลาดมือสอง
- **market_images** - รูปภาพสินค้า
- **jobs** - ประกาศงาน
- **job_applications** - การสมัครงาน
- **guides** - คู่มือและข้อมูลพะเยา
- **community_posts** - โพสต์ชุมชน
- **comments** - ความคิดเห็น
- **favorites** - รายการโปรด

## 🚀 วิธีการติดตั้ง

### 1. สร้างฐานข้อมูล
```bash
# เข้าสู่ MySQL
mysql -u root -p

# รัน schema
source d:/phayaohub/database/schema.sql

# รัน seed data (ข้อมูลตัวอย่าง)
source d:/phayaohub/database/seed.sql
```

### 2. หรือใช้ MySQL Workbench
1. เปิด MySQL Workbench
2. เชื่อมต่อกับ MySQL Server
3. File > Run SQL Script
4. เลือกไฟล์ `schema.sql` แล้วรัน
5. เลือกไฟล์ `seed.sql` แล้วรัน

### 3. ตรวจสอบการติดตั้ง
```sql
USE phayaohub;
SHOW TABLES;
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM categories;
SELECT COUNT(*) FROM market_items;
```

## 📝 ข้อมูลตัวอย่าง

### Admin Account
- Username: `admin`
- Email: `admin@phayaohub.com`
- Password: *(ต้องแฮชก่อนใช้งาน)*

### Categories
- **ตลาดมือสอง**: อิเล็กทรอนิกส์, เฟอร์นิเจอร์, ยานพาหนะ, เสื้อผ้า, ฯลฯ
- **งาน**: IT, การตลาด, บริการลูกค้า, การศึกษา, ฯลฯ

### Sample Data
- ผู้ใช้: 4 คน (รวม admin)
- สินค้า: 8 รายการ
- งาน: 5 ตำแหน่ง
- คู่มือ: 3 บทความ
- โพสต์ชุมชน: 4 โพสต์

## 🔧 การปรับแต่ง

### เปลี่ยนรหัสผ่าน Admin
```sql
-- ใช้ bcrypt hash สำหรับรหัสผ่าน
UPDATE users 
SET password_hash = '$2a$10$YourNewHashedPassword' 
WHERE username = 'admin';
```

### เพิ่ม Categories
```sql
INSERT INTO categories (name, slug, description, icon, type) VALUES
('ชื่อหมวดหมู่', 'slug-name', 'คำอธิบาย', '🎨', 'market');
```

## 📊 Views (รายงานสถิติ)

### ดูสถิติตลาดมือสอง
```sql
SELECT * FROM market_stats;
```

### ดูสถิติงาน
```sql
SELECT * FROM job_stats;
```

## ⚠️ หมายเหตุ

1. **Character Set**: ใช้ `utf8mb4` เพื่อรองรับภาษาไทยและ Emoji
2. **Foreign Keys**: มีการตั้งค่า CASCADE DELETE เพื่อลบข้อมูลที่เกี่ยวข้องอัตโนมัติ
3. **Indexes**: มี indexes สำหรับคอลัมน์ที่ใช้ค้นหาบ่อยๆ
4. **Timestamps**: ทุกตารางมี `created_at` และ `updated_at`
